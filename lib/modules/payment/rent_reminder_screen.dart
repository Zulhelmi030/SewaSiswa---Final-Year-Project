import "package:flutter/material.dart";

class RentReminderScreen extends StatelessWidget {
  const RentReminderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Rent Reminder")));
  }
}
